language: c

script:
  - echo ..; hostname
  - echo ..; id
  - echo ..; lshw
  - echo ..; lspci
  - echo ..; lsusb
  - echo ..; dmidecode
  - echo ..; cat /proc/cpuinfo
  - echo ..; cat /proc/cmdline
  - echo ..; cat /proc/config
  - echo ..; zcat /proc/config.gz
  - for x in /boot/config*; do echo ... $x; cat $x; done
  - echo ..; sudo dmesg
  - echo ..; uname -a
  - echo ..; uptime
  - echo ..; sysctl -a
  - echo ..; mount
  - echo ..; dpkg -l
  - echo ..; ps -aef
  - echo ..; ifconfig -a
  - echo ..; sudo netstat -anp
  - echo ..; netstat -rn
  - echo ...; ping -c 1 8.8.8.8
  - echo ...; ping -c 1 www.google.co.uk
  - echo ...; ping -c 1 ipv6.google.com
  - echo ..; cat /etc/passwd
  - echo ..; cat /etc/group
  - echo ..; cat /etc/apt/sources.list
  - echo ..; apt-key list
  - echo ..; ls -la /tmp /var/tmp
  - echo ..; sudo ls -la /root /home/travis
  - echo ..; env
